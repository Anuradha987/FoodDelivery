import React from 'react';
import { View, Text } from 'react-native';

const OrderDelinpm install react-native-maps --save-exact
very=()=>{

    function renderMap(){
        return(
            <View style={{}}>

            </View>
        )
    }
return(
    <View style = {{flex: 1}}>
        {renderMap()}
    </View>
    )
}

export default OrderDelivery;